function LoginPage() {
  return (
    <section className="panel">
      <p className="eyebrow">Auth</p>
      <h1>로그인</h1>
      <p>이 화면은 다음 단계에서 로그인 API와 연결한다.</p>
    </section>
  );
}

export default LoginPage;
